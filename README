cds-invenio-blogforever git repo
================================

This is a repository for BlogForever-specific Invenio code, templates and configuration files.
The generic Invenio code go to "cds-invenio".
Dirty development code should go to "cds-invenio-dev".

The goal of this repository is to install BlogForever in the following way:

  $ cd cds-invenio
  $ make install
  $ cd ../cds-invenio-blogforever
  $ make install

so that this repository can overwrite or supply additional files to the standard Invenio codebase.

- end of file -
