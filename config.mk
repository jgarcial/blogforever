# $Id$

#PREFIX = /soft/cdsweb
PREFIX = /opt/invenio

BINDIR = $(PREFIX)/bin
ETCDIR = $(PREFIX)/etc
TMPDIR = $(PREFIX)/tmp
LIBDIR = $(PREFIX)/lib
IMGDIR = $(PREFIX)/var/www/img

INSTALL = install -g apache -m 775
