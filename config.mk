# $Id$

#PREFIX = /soft/cdsweb
PREFIX = /opt/invenio

BINDIR = $(PREFIX)/bin
ETCDIR = $(PREFIX)/etc
TMPDIR = $(PREFIX)/tmp
LIBDIR = $(PREFIX)/lib
SQLDIR = $(PREFIX)/lib/sql/invenio
IMGDIR = $(PREFIX)/var/www/img
BINDIR = $(PREFIX)/bin

INSTALL = install -g www-data -m 775
