# $Id$

include ../../config.mk

CSSFILES = invenio.css bull_StylesPlain.css
CSSDIR = $(PREFIX)/var/www/img/

all:
	@echo "Done."

test:
	@echo "Not implemented."

install:
	$(INSTALL) $(CSSFILES) $(CSSDIR)
